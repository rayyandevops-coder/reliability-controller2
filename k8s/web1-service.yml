apiVersion: v1
kind: Service
metadata:
  name: web1-service
spec:
  type: NodePort
  selector:
    app: web1
    version: blue   # 🔵 CURRENT LIVE
  ports:
    - port: 5001
      targetPort: 5001
      nodePort: 30001