apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitor
spec:
  replicas: 2
  selector:
    matchLabels:
      app: monitor
  template:
    metadata:
      labels:
        app: monitor
    spec:
      containers:
      - name: monitor
        image: rayyandevopss/monitor-service:latest
        ports:
        - containerPort: 5004