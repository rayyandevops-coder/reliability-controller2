name: CI-CD Pipeline (Zero Downtime)

on:
  push:
    branches:
      - main

env:
  IMAGE_PREFIX: rayyandevopss

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:

    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Docker Login
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and Push Images
      run: |
        docker build -t $IMAGE_PREFIX/web1-service:${{ github.sha }} ./web1
        docker build -t $IMAGE_PREFIX/web2-service:${{ github.sha }} ./web2
        docker build -t $IMAGE_PREFIX/sarathi-service:${{ github.sha }} ./sarathi
        docker build -t $IMAGE_PREFIX/executer-service:${{ github.sha }} ./executer
        docker build -t $IMAGE_PREFIX/monitor-service:${{ github.sha }} ./monitor

        docker push $IMAGE_PREFIX/web1-service:${{ github.sha }}
        docker push $IMAGE_PREFIX/web2-service:${{ github.sha }}
        docker push $IMAGE_PREFIX/sarathi-service:${{ github.sha }}
        docker push $IMAGE_PREFIX/executer-service:${{ github.sha }}
        docker push $IMAGE_PREFIX/monitor-service:${{ github.sha }}

    - name: Setup kubectl
      uses: azure/setup-kubectl@v3

    - name: Configure Kubernetes
      run: |
        mkdir -p $HOME/.kube
        echo "${{ secrets.KUBECONFIG }}" > $HOME/.kube/config

    - name: Apply Kubernetes Manifests
      run: |
        kubectl apply -f k8s/

    - name: Update Images (Rolling Update)
      run: |
        kubectl set image deployment/web1 web1=$IMAGE_PREFIX/web1-service:${{ github.sha }}
        kubectl set image deployment/web2 web2=$IMAGE_PREFIX/web2-service:${{ github.sha }}
        kubectl set image deployment/sarathi sarathi=$IMAGE_PREFIX/sarathi-service:${{ github.sha }}
        kubectl set image deployment/executer executer=$IMAGE_PREFIX/executer-service:${{ github.sha }}
        kubectl set image deployment/monitor monitor=$IMAGE_PREFIX/monitor-service:${{ github.sha }}

    - name: Verify Rollout
      run: |
        kubectl rollout status deployment/web1 --timeout=60s
        kubectl rollout status deployment/web2 --timeout=60s
        kubectl rollout status deployment/sarathi --timeout=60s
        kubectl rollout status deployment/executer --timeout=60s
        kubectl rollout status deployment/monitor --timeout=60s

    - name: Rollback on Failure
      if: failure()
      run: |
        echo "Rollback triggered"
        kubectl rollout undo deployment/web1
        kubectl rollout undo deployment/web2
        kubectl rollout undo deployment/sarathi
        kubectl rollout undo deployment/executer
        kubectl rollout undo deployment/monitor